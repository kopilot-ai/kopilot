# helm install kopilot ./helm/kopilot -f examples/values-anthropic.yaml \
#   --set anthropic.apiKey=$ANTHROPIC_API_KEY --set api.authToken=$KOPILOT_TOKEN
llm:
  provider: anthropic
  model: claude-opus-5
anthropic:
  model: claude-opus-5
