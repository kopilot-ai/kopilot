{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubedevaiops.serviceAccountName" . }}
  labels:
    {{- include "kubedevaiops.labels" . | nindent 4 }}
{{- end }}
