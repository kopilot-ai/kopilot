apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kubedevaiops.fullname" . }}
  labels:
    {{- include "kubedevaiops.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "kubedevaiops.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kubedevaiops.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "kubedevaiops.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: agent
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.api.port }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.observability.metricsPort }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "kubedevaiops.fullname" . }}-config
            {{- if .Values.existingSecret }}
            - secretRef:
                name: {{ .Values.existingSecret }}
            {{- else }}
            - secretRef:
                name: {{ include "kubedevaiops.fullname" . }}-secrets
                optional: true
            {{- end }}
          {{- if .Values.customSkillsConfigMap }}
          volumeMounts:
            - name: custom-skills
              mountPath: /etc/kubedevaiops/skills
              readOnly: true
          {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- if .Values.customSkillsConfigMap }}
      volumes:
        - name: custom-skills
          configMap:
            name: {{ .Values.customSkillsConfigMap }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
