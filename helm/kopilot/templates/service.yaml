apiVersion: v1
kind: Service
metadata:
  name: {{ include "kopilot.fullname" . }}
  labels:
    {{- include "kopilot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kopilot.selectorLabels" . | nindent 4 }}
